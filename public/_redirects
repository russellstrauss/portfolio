# Catch-all for Vue Router routes (SPA)
# Cloudflare Pages should serve static files (with extensions) automatically
# before processing redirects, so this should only match Vue routes
/*    /index.html   200

